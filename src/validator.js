const validator = {
 

     validateNum(num){
        booleano = false
        if(num>=0){
            booleano= false
        }else{
            booleano= true
        }
        return booleano
    }

};

export default validator;
